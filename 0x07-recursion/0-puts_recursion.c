#include "holberton.h"

/**
 * _puts_recursion - A function that prints a string, followed by 
 * new line.
 * @s: pointer to string
 * Return: nothing
 */

void _puts_recursion(char *s)
{
	int start = 0;
	char ch;
	
	if (s == '\0')
	{
		return;
		_puts_recursion(
	}
}
